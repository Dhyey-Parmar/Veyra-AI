# Veyra Frontend Environment Configuration
#
# Production (Vercel):
# Leave empty to use same-origin relative '/api' endpoints.
VITE_API_URL=

# Local Development:
# Uncomment if connecting to a separate backend instance without proxy:
# VITE_API_URL=http://localhost:8000/api
