{
  "name": "veyra",
  "version": "1.0.0",
  "private": true,
  "description": "Veyra - Intelligent Credit Risk Analytics",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "cd frontend && npm install && npm run build",
    "preview": "npm --prefix frontend run preview"
  },
  "devDependencies": {
    "vite": "^5.4.14"
  }
}
